// Setup
let sum = 0;

function addThree() {
  sum = sum + 3;
}

// Modifica il codice solo sotto questa riga

function addFive(){
  sum += 5;
}
// Modifica il codice solo sopra questa riga

addThree();
addFive();