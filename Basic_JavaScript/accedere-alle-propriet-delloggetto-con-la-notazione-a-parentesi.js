// Setup
const testObj = {
  "an entree": "hamburger",
  "my side": "veggies",
  "the drink": "water"
};

// Modifica il codice solo sotto questa riga
const entreeValue = testObj["an entree"];   // Modifica questa riga
const drinkValue = testObj["the drink"];    // Modifica questa riga