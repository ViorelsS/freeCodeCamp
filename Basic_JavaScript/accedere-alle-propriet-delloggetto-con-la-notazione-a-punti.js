// Setup
const testObj = {
  "hat": "ballcap",
  "shirt": "jersey",
  "shoes": "cleats"
};

// Modifica il codice solo sotto questa riga
const hatValue = testObj.hat;      // Modifica questa riga
const shirtValue = testObj.shirt;    // Modifica questa riga